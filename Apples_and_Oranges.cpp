#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, x, y, z, m, n;
    int t, i, count = 0;
    string str;

    cin >> x;
    cin >> a >> b;
    if (a + b <= x)
        cout << "Yes" << endl;
    else
        cout << "No" << endl;

    return 0;
}