#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, x, y, z, m, n;
    int t, i, count = 0;
    string str;
    cin >> t;
    while (t--)
    {
        cin >> str;
        n=str.length();
        int arr[n];
        
        for (i = 0; i < n; i++)
        {
            cin >> arr[i];
           
        }
        cout << count << endl;
    }

    return 0;
}