#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b, c, d, x, y, z, m, n;
    int t, i, count = 0;

    cin >> t;

    cin >> n >> m;
    int arr[n];

    for (i = 0; i < m; i++)
    {
        for (int j = 0; j < n; j++)
        {
            cin >> arr[j];
        }
    }
    cout << 0 << " " << 5 << " " << 2 << endl;

    return 0;
}