#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long int a, b, c, x, y, m, n, r;
    int t, i, count = 0;
    string str;
    cin >> t;
    while (t--)
    {
        cin >> x >> y >> n >> r;
        int n1, n2;
        n1 = r / x;
        n2 = r / y;

        if (n1 >= n || n2 >= n)
        {
            for (i = 0; n1 + n2 != n; i++)
            {
                
            }
        }
    }

    return 0;
}