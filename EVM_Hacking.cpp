#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b, c, p, q, r, n;
   
    cin >> t;
    while (t--)
    {
        cin >> a >> b >> c >> p >> q >> r;
    

        for (i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
    }
    return 0;
}