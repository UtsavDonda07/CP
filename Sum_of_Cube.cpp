#include <bits/stdc++.h>
using namespace std;
int main()
{

    long long int t, n;
    cin >> t;
    while (t--)
    {
        cin >> n;
        long long int arr[n];

        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
        long long int mul = 1;
        long long int sum = 0;
        for (int i = 0; i < n; i++)
        {
            for (int j = i; j < n; j++)
            {
                
            }
        }
    }

    return 0;
}