#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b, c, d, x, y, z, m, n;
    int t, i, count = 0;
    string str;

    cin >> t;
    while (t--)
    {
        cin >> n;
        int arr[n];
cout << "hello " << endl;
        for (i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
    }
    
    return 0;
}
