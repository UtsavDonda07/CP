#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b, c, d, x, y, z, m, n;
    int t, i, count = 0;

    cin >> n;
    int arr[n];
    map<int, int> m;
    for (i = 0; i < n; i++)
    {
        int x;
        cin >> x;
        m[x]++;
    }

    return 0;
}