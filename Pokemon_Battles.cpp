#include <bits/stdc++.h>
using namespace std;
int main()
{
    int m, n, x, y, z;
    int t, i;

    cin >> t;
    while (t--)
    {
        cin >> n;

        int a[n], b[n];
        for (i = 0; i < n; i++)
        {
            cin >> a[i];
        }

        for (i = 0; i < n; i++)
        {
            cin >> b[i];
        }
       for (int i = 0; i < n; i++)
       {
     
       }
       
    }
    return 0;
}