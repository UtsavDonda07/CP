#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b, c, d, x, y, z, m;
    int t, i, count = 0, n;
    string str;

    cin >> t;
    while (t--)
    {
        cin >> n;

        int arr[n];

        for (i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
    }
    return 0;
}